name = "vkrama-store"
compatibility_date = "2025-02-04"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

# D1 and R2 bindings configured in Cloudflare Pages Dashboard:
# Settings → Functions → D1/R2 database bindings
